FUNCTION DFS(start, visited, order):
    IF visited is empty:
        CREATE empty visited set
        CREATE empty order list

    ADD start to visited
    ADD start to order

    FOR each neighbor of start:
        IF neighbor not in visited:
            CALL DFS(neighbor, visited, order)  ← recursive call

    RETURN order